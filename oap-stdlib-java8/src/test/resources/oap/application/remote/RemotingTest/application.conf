{
  profiles = [remoting, http-io]
  services {

    remoting-listener.parameters.keystoreLocation = "src/test/resources/oap/application/remote/RemotingTest/server_keystore.jks"
    remoting-listener.parameters.keystorePassword = "123456"

    remoting.parameters.serialization = JSON
  }
}
