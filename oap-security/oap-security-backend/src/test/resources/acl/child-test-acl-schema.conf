{
  parentPath = root
  permissions = [
    "organization.create"
  ]
  children {
    user {
      permissions = [
        "user.read",
        "user.update",
        "user.delete"
      ]
    }
    organization {
      permissions = [
        "organization.read",
        "organization.update",
        "organization.delete",
        "user.create",
        "user.invite"
      ]
      children {
        user {
          permissions = [
            "user.read",
            "user.update",
            "user.delete"
          ]
        }
      }
    }
  }
}