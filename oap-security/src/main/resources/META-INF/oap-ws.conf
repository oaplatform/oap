{
  services {
    organization {
      service = ws-organization
      protocol = HTTP
    }
    login {
      service = ws-login
      protocol = HTTP
    }
    auth {
      service = ws-auth
      protocol = HTTP
    }
  }
}
