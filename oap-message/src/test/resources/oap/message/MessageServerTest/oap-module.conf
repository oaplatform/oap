name = oap-message-test

dependsOn = [
  oap
  oap-message
]

services {
  message-listener-mock {
    profiles = [
      oap-message-server
    ]
    implementation = oap.message.MessageListenerMock
    parameters {
      messageType = 12
    }
    link.listeners = <modules.oap-message.oap-http-message-handler>
  }
}
