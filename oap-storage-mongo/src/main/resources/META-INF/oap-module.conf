{
  name = oap-storage-mongo
  services {
    migration {
      profile = mongo-migration
      implementation = oap.storage.mongo.DirectoryMigration
      parameters {
//        directory = /tmp/migration
      }
      supervision.supervise = true
    }
  }
}
