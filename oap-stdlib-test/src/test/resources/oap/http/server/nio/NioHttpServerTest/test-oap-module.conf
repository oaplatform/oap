name = test-oap

dependsOn = oap

services {

  test-handler {
    implementation = oap.http.server.nio.NioHttpServerTest.TestHttpHandler
    parameters {
      server = modules.oap.oap-http-server
      prefix = /test
      port = default-https
    }
  }
}
