{
  name = ws-profile
  services {
    test-no-profile {
      service = no-profile
      protocol = HTTP
    }
    test-with-profile {
      service = with-profile
      profile = test-profile
      protocol = HTTP
    }
    new-profile {
      service = with-profile
      profile = new-profile
      protocol = HTTP
    }
  }
}
