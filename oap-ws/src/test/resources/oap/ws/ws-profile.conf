{
  services {
    test-no-profile {
      service = no-profile
      protocol = HTTP
    }
    test-with-profile {
      service = with-profile
      protocol = HTTP
    }
    not-configured-profile {
      service = profile-not-configured
      protocol = HTTP
    }
  }
  profiles = [test-profile]
}
