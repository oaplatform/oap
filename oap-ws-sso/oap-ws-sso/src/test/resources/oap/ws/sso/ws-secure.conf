{
  name = ws-secure
  services {
    auth {
      service = auth
      protocol = HTTP
      sessionAware = true
    }
    secure {
      service = secure
      protocol = HTTP
      sessionAware = true
    }
  }
  interceptors = [security]
}
