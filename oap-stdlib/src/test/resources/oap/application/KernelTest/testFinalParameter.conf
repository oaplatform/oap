name = testFinalParameter
services {
  service3 {
    implementation = oap.application.KernelTest.ServiceFinalParameter
    parameters {
      a = new value // pubilc final String
      al = ["val1"]
      extra = constructor param
    }
  }
}
