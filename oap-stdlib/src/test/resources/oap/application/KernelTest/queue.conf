name = queue
services {
  queue {
    implementation = java.util.concurrent.LinkedBlockingQueue
    parameters.arg0 = ["item1", "item2"]
  }

  container {
    implementation = oap.application.KernelTest.TestQueueContainer
    parameters.queueArg = modules.this.queue
    parameters.queueField = modules.this.queue
  }
}
