{
  name = oap-http
  services {
    oap-http-server {
      implementation = oap.http.Server
      parameters {
        port = 8880
        workers = 500
        default-headers = {
          Access-Control-Allow-Origin: "*"
        }
      }
      supervision.supervise = true
    }
  }
}
