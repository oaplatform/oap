boot.main = oap-module-with-remoting

profiles = [
  http
  remoting
]

services {
  oap-http {
    oap-http-server.parameters {
      defaultPort.httpPort = ${REMOTE_FIXTURE_HTTP_PORT}
      additionalHttpPorts.httpprivate = ${REMOTE_FIXTURE_HTTP_PORT}
    }
  }
  oap-application {
    remoting.parameters.serialization = JSON
  }
}
